# generated by Gemini
services:
  homepage:
    build:
      context: .
      dockerfile: apps/homepage/Dockerfile
    ports:
      - "4321:4321"
    volumes:
      - ./apps/homepage:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0

  tech-memo:
    build:
      context: .
      dockerfile: apps/tech-memo/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./apps/tech-memo:/app
      - /app/node_modules
